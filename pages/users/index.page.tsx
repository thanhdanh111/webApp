import React, { } from 'react';
import ListUsers from './UI/users';

const Login = () => {
  return (
        <React.Fragment>
            <ListUsers />
        </React.Fragment>
  );
};

export default Login;
