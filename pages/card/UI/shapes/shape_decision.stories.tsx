import { storiesOf } from '@storybook/react';
import React from 'react';
import CustomDecision from './shape_decision';

storiesOf('ShapeDecision Scripts', module).add('ShapeDecision Scripts', () => {
  return (
    <>
      <CustomDecision />
    </>
  );
});
