import { storiesOf } from '@storybook/react';
import React from 'react';
import CreateBoard from './create_board';

storiesOf('CreateBoard Scripts', module).add('CreateBoard Scripts', () => {
  return (
    <>
        <CreateBoard />
    </>
  );
});
