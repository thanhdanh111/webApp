import { storiesOf } from '@storybook/react';
import React from 'react';
import ViewBoard from './view_board';

storiesOf('NewBoard Scripts', module).add('NewBoard Scripts', () => {
  return (
    <>
      <ViewBoard />
    </>
  );
});
